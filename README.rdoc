== simple_notes

A simple rails app for storing arbitrary user text content.

* update all gems: <tt>bundle update</tt>
* run all migrations: <tt>rake db:migrate</tt>


display with: <tt>rake doc:app</tt>
